// review script from debuggercises
// request for a .js file and get back an html file with the code + terminal output
// generalize the console capturing to simply indicate what method was called

module.exports = (req, res, config) => ({ req, res });
