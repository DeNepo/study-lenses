# Parameters

```js
// one parameter named `param`
const thingy = (param) => {
  return param; // <-- returns the parameter
};

const four = thingy(4); // <-- assigns the return value to `four`

const six = thingy(6); // <-- assigns the return value to `six`
```
